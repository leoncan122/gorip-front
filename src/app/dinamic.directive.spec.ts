import { DinamicDirective } from './dinamic.directive';

describe('DinamicDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicDirective();
    expect(directive).toBeTruthy();
  });
});
